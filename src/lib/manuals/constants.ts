/** One-time fee for paid owner-manual lookup (cents). */
export const MANUAL_PAID_LOOKUP_FEE_CENTS = 299;

export const MANUAL_PAID_LOOKUP_FEE_LABEL = "$2.99";

export const MANUAL_PAID_PROVIDER = "vehicle_databases";

/** Max owner-manual PDF size when archiving to GCS. */
export const MAX_OWNER_MANUAL_PDF_BYTES = 50 * 1024 * 1024;
